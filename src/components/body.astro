---
import { twMerge } from "tailwind-merge";

interface Props {
  justify?: boolean;
}

const { justify } = Astro.props;
---

<body
  class={twMerge(
    justify
      ? "bg-primary dark:bg-primary-dark text-zinc-900 dark:text-zinc-50 break-words leading-6 overflow-x-hidden min-h-screen flex flex-col justify-between"
      : "bg-primary dark:bg-primary-dark text-zinc-900 dark:text-zinc-50 break-words leading-6 overflow-x-hidden min-h-screen"
  )}
>
  <slot />
</body>
